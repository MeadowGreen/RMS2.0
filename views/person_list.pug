extends layout

block content
   #main
      h2 Person List
      .card-body
        ul
  each person in person_list
    li 
      a(href=person.url) #{person.name} 
      | (#{person.lifespan})

  else
    li There are no persons.